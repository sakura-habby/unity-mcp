# Unity MCP 多实例支持改造任务清单

## Phase 1: 核心基础设施 ✅
- [x] 1.1 创建 UnityInstanceInfo 数据类 (`Server/models.py`)
- [x] 1.2 实现 discover_all_unity_instances() (`Server/port_discovery.py`)
- [x] 1.3 实现 UnityConnectionPool 类 (`Server/unity_connection.py`)
- [x] 1.4 更新 server.py 使用连接池

## Phase 2: 工具层适配 ✅
- [x] 2.1 创建 list_unity_instances 工具
- [x] 2.2 更新 manage_scene 添加 unity_instance 参数
- [x] 2.3 更新 manage_asset 添加 unity_instance 参数
- [x] 2.4 更新 manage_gameobject 添加 unity_instance 参数
- [x] 2.5 更新 manage_script 添加 unity_instance 参数
- [x] 2.6 更新其他工具添加 unity_instance 参数
  - [x] manage_editor.py
  - [x] manage_shader.py
  - [x] read_console.py
  - [x] execute_menu_item.py
  - [x] manage_prefabs.py
  - [x] script_apply_edits.py
  - [x] run_tests.py
  - [x] resource_tools.py (list_resources, read_resource, find_in_file)

## Phase 3: Unity端改进 ✅
- [x] 3.1 在心跳文件中添加项目名称和Unity版本
- [x] 3.2 Stop() 时删除状态文件

## Phase 4: 配置和文档 ✅
- [x] 4.1 添加环境变量支持 (UNITY_MCP_DEFAULT_INSTANCE)
- [x] 4.2 添加命令行参数支持 (--default-instance)
- [x] 4.3 更新文档
  - [x] MULTI_INSTANCE_GUIDE.md (中文)
  - [x] MULTI_INSTANCE_GUIDE_EN.md (英文)

## 修复和优化 ✅
- [x] 修复帧格式协议的端口探测
- [x] 修复 Unity 端口冲突问题 (SO_REUSEADDR)
- [x] 更新诊断和清理脚本

## Phase 5: MCP Resources 多实例支持 ✅
- [x] 5.1 添加查询参数支持 (使用 RFC 6570 {?unity_instance} 语法)
  - [x] menu_items.py - get_menu_items
  - [x] tests.py - get_tests
  - [x] tests.py - get_tests_for_mode
- [x] 5.2 升级 FastMCP 版本要求到 >=2.13.0 (查询参数支持的最低版本)
- [x] 5.3 实现双版本注册机制（向后兼容）
  - [x] 带查询参数的模板版本 (resources/templates/list)
  - [x] 不带参数的固定版本 (resources/list，使用默认实例)

## 当前进度
✅ 所有阶段完成！

## 总结
- **21 个 MCP Tools** 全部支持 `unity_instance` 参数
  - 20 个需要与 Unity 通信的工具
  - 1 个本地工具 (manage_script_capabilities)
- **3 个 MCP Resources** 全部支持 `{?unity_instance}` 查询参数
  - menu_items: `mcpforunity://menu-items{?unity_instance}`
  - tests: `mcpforunity://tests{?unity_instance}`
  - tests_for_mode: `mcpforunity://tests/{mode}{?unity_instance}`
  - **注意**: 查询参数需要 FastMCP >= 2.13.0
- **连接池** 支持自动发现和管理多个 Unity 实例
- **端口冲突** 问题已完全解决
- **状态文件** 自动清理机制已实现

## 使用示例

### MCP Resources 访问方式

每个 resource 提供**两种访问方式**：

#### 方式 1：固定 URI（单实例/默认，向后兼容）
```python
# 这些 URI 出现在 resources/list 中，直接使用默认实例
mcpforunity://menu-items
mcpforunity://tests
mcpforunity://tests/EditMode
```

#### 方式 2：模板 URI（多实例，带查询参数）
```python
# 这些 URI 出现在 resources/templates/list 中，可指定实例
mcpforunity://menu-items{?unity_instance}
mcpforunity://tests{?unity_instance}
mcpforunity://tests/{mode}{?unity_instance}

# 使用示例
mcpforunity://menu-items?unity_instance=MyProject
mcpforunity://tests?unity_instance=GameProject@abc123
mcpforunity://tests/EditMode?unity_instance=MyProject
```

### 注册的 Resources

| 固定 URI (默认实例) | 模板 URI (可指定实例) | Resource Name |
|---------------------|---------------------|---------------|
| `mcpforunity://menu-items` | `mcpforunity://menu-items{?unity_instance}` | `get_menu_items_default` / `get_menu_items` |
| `mcpforunity://tests` | `mcpforunity://tests{?unity_instance}` | `get_tests_default` / `get_tests` |
| `mcpforunity://tests/{mode}` | `mcpforunity://tests/{mode}{?unity_instance}` | `get_tests_for_mode_default` / `get_tests_for_mode` |
